<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body { font-family: 'Inter', sans-serif; color: #0d0c22; }
        .input-focus:focus { 
            border-color: #ea4c89; 
            box-shadow: 0 0 0 4px rgba(234, 76, 137, 0.1); 
            outline: none;
        }
    </style>
    <title>Login - Modern Blog</title>
</head>

<body class="bg-white min-h-screen">
    <%- include('./partials/nav') %>

    <div class="flex items-center justify-center py-20 px-6">
        <div class="w-full max-w-md">
            <div class="text-center mb-10">
                <h2 class="text-3xl font-bold mb-2">Sign in to Blog</h2>
                <p class="text-gray-500">Enter your details below</p>
            </div>

            <form action="/user/signin" method="post" class="space-y-6">
                <div>
                    <label for="email" class="block text-sm font-bold mb-2">Email address</label>
                    <input type="email" 
                           name="email" 
                           id="email" 
                           placeholder="name@example.com"
                           class="w-full px-4 py-3 bg-[#f3f3f4] border border-transparent rounded-lg text-sm transition-all input-focus" 
                           required>
                </div>

                <div>
                    <div class="flex justify-between mb-2">
                        <label for="password" class="text-sm font-bold">Password</label>
                    </div>
                    <input type="password" 
                           name="password" 
                           id="password" 
                           placeholder="password"
                           class="w-full px-4 py-3 bg-[#f3f3f4] border border-transparent rounded-lg text-sm transition-all input-focus" 
                           required>
                </div>

                <button type="submit" 
                        class="w-full bg-[#ea4c89] text-white py-3 rounded-full font-bold text-sm hover:bg-[#f082ac] transition-colors shadow-lg shadow-pink-100">
                    Sign In
                </button>
            </form>

            <p class="mt-8 text-center text-sm text-gray-600">
                Not a member? 
                <a href="/user/signup" class="text-[#ea4c89] font-medium hover:underline">Sign up now</a>
            </p>
        </div>
    </div>

    <%- include('./partials/scripts') %>
</body>

</html>